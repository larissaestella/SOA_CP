package com.example.reservas.entity;

/**
 * Enumeração representando o status de uma sala.
 * Uma sala pode estar ativa (disponível para reservas) ou inativa (indisponível).
 */
public enum StatusSala {
    ATIVA,
    INATIVA
}
