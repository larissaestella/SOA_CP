package com.example.reservas.entity;

/**
 * Enumeração representando o status de uma reserva.
 * Utilizamos ATIVA quando a reserva está agendada e CANCELADA quando foi cancelada.
 */
public enum StatusReserva {
    ATIVA,
    CANCELADA
}
